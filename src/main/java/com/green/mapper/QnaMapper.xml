<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org/DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.green.mapper.QnaMapper">
	<select id="listqna" resultType="com.green.vo.QnaVO" >
		select * from qna_tbl order by qno
	</select>
	<select id="test" resultType="String" >
		select writer from qna_tbl where qno=#{qno}
	</select>

	<insert id="insertQna">
		insert into qna_tbl(qno, title, writer,reg_date,pwd,p_group) 
			values(qna_seq.nextval, #{title},#{writer},#{reg_date},#{pwd},#{p_group})
	</insert>
</mapper>