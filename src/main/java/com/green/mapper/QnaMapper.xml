<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org/DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.green.mapper.QnaMapper">
	<select id="listqna" resultType="com.green.vo.QnaVO" >
		select * from qna_tbl order by qno
	</select>

	<insert id="insertQna">
		insert into qna_tbl(qno, title, writer,q_content,reg_date,pwd,p_group) 
			values(qna_seq.nextval, #{title},#{writer},#{q_content},#{reg_date},#{pwd, jdbcType=VARCHAR},#{p_group})
	</insert>
	
	<select id="get" resultType="com.green.vo.QnaVO" >
		select * from qna_tbl where qno=#{qno}
	</select>
</mapper>