<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org/DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.green.mapper.StudyMapper">

	
	<select id="studySelect" resultType="com.green.vo.StudyVO" >
		select * from tbl_group order by sno desc
	</select>

	<insert id="studyInsert">
		insert into tbl_group(sno, group_name, leader,subject,description,member_number) 
			values(seq_sno.nextval, #{group_name},#{leader},#{subject},#{description},#{member_number})
	</insert>
</mapper>