<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org/DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.green.mapper.BoardMapper">
	<insert id="register">
		<selectKey keyProperty="bno" order="BEFORE" resultType="long">
			select seq_bno.nextval from dual
		</selectKey>
		insert into tbl_board(bno, group_name, title, content, writer, regdate)
			values(#{bno}, #{group_name}, #{title}, #{content}, #{writer}, sysdate)
	
	</insert>
	
	<select id="showList" resultType="com.green.vo.BoardVO">
		select * from tbl_board where group_name=#{group_name}
	</select>
	
	<select id="read" resultType="com.green.vo.BoardVO">
		select * from tbl_board where bno=#{bno}
	</select>
	
	<update id="update">
		update tbl_board 
			set group_name=#{group_name}, title=#{title}, content=#{content}, writer=#{writer}
				where bno=#{bno}
	</update>
	
	<delete id="delete">
		delete from tbl_board where bno=#{bno}
	</delete>
</mapper>

